# 작업기록 — 2026-08-08

> 포스트 2편을 한/영 쌍으로 발행한 세션. 에이전트 전문가화 방법론, 그리고 2D 라이다 V홈 도킹.
> 곁가지로 영상 편집(트림 + 정지 연장)과 웹용 변환을 했다.
> 핵심 교훈 셋: **HEVC 는 브라우저에서 안 돈다**, **concat `-c copy` 는 인코더가 다르면 깨진다**,
> **초안 디렉터리의 대용량 원본은 `publishResources: false` 로 저장소에만 남긴다.**

---

## 0. 커밋

| 커밋 | 내용 |
|---|---|
| `22bd930` | 에이전트 전문가화 방법론 포스트 (한/영 + SVG 4) |
| `9a5a922` | 소스 초안 2편 (08-07 랜드스케이프, 08-08 방법론) |
| `a4935b6` | 2D 라이다 V홈 도킹 포스트 (한/영 + SVG 6 + 영상 2) |
| `2936b95` | 0808 초안 원본 영상 보관 + 배포 산출물에서 제외 |

---

## 1. Hivemoot 실측 진단

08-07 초안이 "빈칸에 가장 근접한 사례"로 지목한 `hivemoot/hivemoot` 을 GitHub API 로 재확인했다.
초안의 수치(스타 16 / 이슈 96 / 푸시 2026-07-09)는 **한 달 뒤에도 전부 그대로**였다.

추가로 알아낸 것:

- **이슈 96 = 순수 이슈 75 + 열린 PR 21.** "이슈 96" 만 쓰면 PR 이 섞인 수치다.
- 릴리스 **0건**.
- 기본 브랜치 마지막 실질 커밋 **2026-05-31**(author `Queen`), 이후는 전부 dependabot.
- 워처 **1명** — 커뮤니티가 형성되지 않았다는 직접 증거.

> **`pushed_at` 으로 활성도를 판단하지 말 것.** 비기본 브랜치 활동까지 잡아서 실제보다 최신으로 보인다.
> 기본 브랜치의 non-bot 커밋을 봐야 한다. 이 경우 07-09 vs 05-31 로 6주 차이가 났다.

이 진단은 초안에만 기록했고 **발행하지 않았다.** 갈래 (a) 로 따로 낼 재료로 남아 있다.

---

## 2. 포스트 1 — 에이전트 전문가화 방법론

`/posts/ai-agent/에이전트-전문가화-파인튜닝은-9번째/` + `/en/posts/ai-agent/agent-domain-expertise/`

08-08 초안의 갈래 **(b) 방법론 단독**으로 발행. 1장(Hivemoot 실측)은 뺐다 — 실측과 의견을
한 편에 섞으면 검증 부담이 커진다.

구성: 네 결핍 진단(지식·절차·판단·접지) → 9단 비용 사다리 → 자료 티어 → 학습보다 효율적인 것 7가지
→ 블로그 적용 → **한계**.

- 초안 §8 의 미검증 항목을 6장에 그대로 옮겼다. "사다리 순서는 벤치마크가 아니라 판단",
  "검증기 정밀도가 낮으면 성립 안 함" 등. **의견을 실측처럼 보이게 두지 않는 게 이 블로그 스타일이다.**
- SVG 2종 × ko/en. `①②③④` 는 SVG 에서 평문 숫자로 바꿨다 — 영문 독자 환경에 CJK 폰트가
  없으면 브라우저에서도 tofu 가 날 수 있다. 마크다운 한국어판에는 그대로 뒀다.

---

## 3. 영상 작업 — 라이다 도킹 클립

발행 전에 원본 영상을 다듬었다. 여기서 시간이 가장 많이 들었다.

### 3-1. 트림은 무손실, 정지 연장은 재인코딩

앞 18초만 남기는 것은 `-t 18 -c copy` 로 무손실. 문제는 **마지막 프레임을 4초 정지로 잇는 것**이었다.

처음 시도한 무손실 concat 이 실패했다.

```text
[hevc] Duplicate POC in a sequence: 0
[hevc] Could not find ref with POC 2
프레임 1055 / 기대 1265
```

원인: **concat 데뮤서 + `-c copy` 는 두 세그먼트의 HEVC 파라미터셋(SPS/PPS)이 같아야 한다.**
원본(기기 인코더)과 정지 클립(libx265)은 extradata 가 달라서, 컨테이너에는 첫 파일의 것만 남고
두 번째 구간이 잘못된 SPS 로 디코딩된다.

→ `tpad` 필터로 단일 패스 재인코딩으로 전환.

```bash
ffmpeg -t 18 -i src.mp4 \
  -vf "tpad=stop_mode=clone:stop_duration=4" -af "apad=pad_dur=4" \
  -c:v libx265 -crf 18 -tag:v hvc1 -c:a aac out.mp4
```

**`-t` 를 출력 옵션에 두면 정지 구간까지 잘린다.** 처음에 `-i` 뒤에 뒀다가 22초가 아니라 18.01초가
나왔다. 입력 측(`-i` 앞)으로 옮겨야 트림 후 연장이 된다.

### 3-2. 정지 검증은 해시가 아니라 PSNR

정지 구간 프레임을 md5 로 비교했더니 전부 달랐다. 손실 압축이라 당연하다.
PSNR 로 재니 판별이 됐다.

| 비교 | PSNR |
|---|---:|
| 정지 구간 내부 (18.3s ↔ 20.0s) | 66.3 dB |
| 정지 구간 내부 (20.0s ↔ 22.0s) | 42.6 dB |
| **실제 움직이는 구간 (17.5s ↔ 18.3s)** | **17.5 dB** |

자릿수가 다르므로 정지가 걸린 게 맞다. (`psnr` 필터 출력은 PNG 입력이면 `y:u:v:` 가 아니라
`r:g:b:` 로 나온다 — grep 패턴 주의.)

### 3-3. 회전 메타데이터

원본은 코딩 1920×1080 + `displaymatrix: -90°` 였다. 프레임을 PNG 로 뽑으면 자동 회전이 걸려
1080×1920 이 나오므로, 코딩 해상도를 맞추려면 `-noautorotate` 가 필요하다.
재인코딩하면 세로로 똑바로 구워지고 회전 메타데이터가 사라진다 — 보이는 화면은 같고,
회전을 무시하는 플레이어에서는 오히려 안전하다.

---

## 4. 포스트 2 — 2D 라이다 V홈 도킹

`/posts/auto_drive/카메라없이-2d라이다-v홈-도킹/` + `/en/posts/auto_drive/lidar-vgroove-docking/`

`0808/` 초안(코드 포함판)을 발행본으로 변환. 원문 7부 구조를 유지했다.

### 4-1. 다이어그램 3종

| SVG | 내용 |
|---|---|
| `autodrive-vgroove-pipeline` | 스캔 → 클러스터 → 직선분해 → 쌍찾기 → 자세산출 |
| `autodrive-vgroove-apex-intersection` | 교점 σ 0.33 mm vs 팔 끝 σ 2.3 mm |
| `autodrive-vgroove-60-vs-120` | 같은 두 직선이 만드는 60°/120° 모호성 (원문 박스문자 대체) |

**`60-vs-120` 은 한 번 다시 그렸다.** 처음엔 두 패널의 직선 기울기가 서로 달라서
"두 직선이 완전히 동일하다" 는 요지가 그림에서 안 보였다. **동일한 X자를 양쪽에 그리고
서로 다른 쐐기를 강조하는** 형태로 고쳤다. 손수 쓴 SVG 는 래스터라이즈해서 봐야 이런 걸 잡는다.

### 4-2. 영상 임베드

`auto_drive` 포스트의 기존 관례는 480px GIF 인데, 22초·35초 세로 영상은 GIF 로 만들면 몇 배로 커진다.
갤러리에서 이미 쓰는 `<video controls loop muted playsinline>` 방식을 택했다(`goldmark.unsafe: true`).

**원본은 HEVC 45MB / H.264 31MB 였다. HEVC 는 브라우저에서 재생되지 않는다.**
둘 다 H.264 540p 30fps 로 재인코딩(합계 3.6MB)해 `static/images/auto_drive/` 에 뒀다.

---

## 5. 초안 원본 76MB — 저장소에는 남기고 배포에서는 빼기

초안 디렉터리의 원본 영상 2개가 합계 76MB 였다(저장소 최대 파일의 2배).
그냥 커밋하면 **빌드 산출물로도 복사되어 GitHub Pages 로 배포된다.**

`_index.md` 의 `build` 와 `cascade.build` 양쪽에 `publishResources: false` 를 넣어 해결했다.

```yaml
build:
  render: never
  list: never
  publishResources: false
cascade:
  build:
    render: never
    list: never
    publishResources: false
```

검증: `Non-page files` 17 → 13(0808 원본만 빠짐), 웹용 사본은 산출물에 유지, URL 465개 변동 0건.

---

## 6. 검증 게이트 — 이번에 고친 것

두 포스트 모두 동일한 게이트를 통과시켰다.

| 항목 | 포스트 1 | 포스트 2 |
|---|---|---|
| 기존 URL 삭제·변경 | 0건 (402→442) | 0건 (442→465) |
| 빌드 WARN / ERROR | 0 / 0 | 0 / 0 |
| 목록 ko / en | 35→36 동시 | 36→37 동시 |
| goat 렌더 | 7편 유지 | 7편 유지 |
| 언어 없는 코드펜스 | 0 (`<pre>` 2 == 펜스 2) | 0 (`<pre>` 27 == 펜스 27) |
| 참조 에셋 | 4/4 | 8/8 |

> **목록 항목 수 검증은 1페이지만 세면 안 된다.** `/en/posts/index.html` 만 grep 하면
> 페이지네이션 때문에 before/after 가 똑같이 10 으로 나와 **통과한 것처럼 보인다.**
> `page/*/index.html` 까지 합산해야 35→36 같은 실제 증가가 보인다(`page/1` 은 index 의 별칭이라 제외).

URL 기준선은 저장소 `public/` 이 아니라 **신규 파일을 잠시 밖으로 `mv` → 빌드 → 되돌리기 → 빌드**
A/B 로 만들었다.

---

## 7. 환경 메모

- 시스템에 `ffmpeg` 이 없다. sudo 없이 쓰려면 스크래치패드 venv 에 `imageio-ffmpeg` 를 깔면
  정적 바이너리(7.0.2)가 딸려 온다. `imageio_ffmpeg.get_ffmpeg_exe()` 로 경로를 얻는다.
- `ffmpeg` 은 `-y` 가 없으면 기존 파일을 덮어쓰지 않고 실패한다. stderr 를 `/dev/null` 로 보내면
  **실패를 모른 채 이전 산출물을 계속 쓰게 된다.** 이번에 세로/가로 해상도가 안 맞았던 원인이 이것이었다.
- SVG 래스터라이즈는 `cairosvg`(같은 venv). 한글이 □ 로 나오는 건 래스터라이저 폰트 문제이지
  SVG 결함이 아니다 — **배치만 본다.**

---

## 남은 것

- **Hivemoot 정지 진단(초안 1장)은 미발행.** 갈래 (a) 로 짧게 낼 수 있는 실측 재료.
- 08-07 랜드스케이프 초안의 미검증 항목은 그대로다 — Petals 스웜 실가동률, Prime Intellect 의
  소비자 GPU 허용 여부, Moltbook/Agentverse 에이전트 수치.
- goat 렌더 문제 7편은 이번에도 손대지 않았다(늘지는 않았다).
- 라이다 도킹 포스트의 영상 2편은 **실제 브라우저 재생을 확인하지 않았다.** 경로·태그까지만 검증했다.
