# 작업기록 — 2026-06-21

> MOCA 카페 로봇 프로젝트(Confluence 익스포트)를 면접 대비로 학습 → 시스템 설계/통합 관점 정리 → 블로그 2부작 포스트 발행 세션.
> 핵심 교훈: **발행은 `~/skong097.github.io` repo 에서만** (myblog 아님). **박스 다이어그램(`┌─┐│`)은 코드펜스 금지 → SVG `{{< figure >}}` 변환** (CLAUDE.md 규칙).

---

## 0. 배경 — MOCA 프로젝트 학습 (면접 준비)

`~/dev_ws/moca_confluence/output/RAP` 의 Confluence 익스포트(167 HTML) 학습.
- MOCA(MObile CAtering Snack Bar Service) = 행사장 파견형 이동식 자율 카페 로봇. 팀 Dynamic Robotics.
- 본인 담당 = **운영 웹 대시보드(웹앱) + OpServer + 5-State FSM 설계** (관제/운영 레이어 풀스택).
- 면접 학습노트 2종 작성(해당 작업 폴더): `MoCa_시스템설계_면접노트.md`, `MoCa_런타임동작흐름_노트.md`.

---

## 1. 블로그 2부작 포스트 작성

**위치**: `content/posts/robotics/` (category: robotics)

| 편 | 파일 | 내용 |
|---|---|---|
| 1편 설계편 | `moca-시스템-아키텍처-웹운영화면부터-ros2-로봇까지.md` | 3계층(UI/Service/Device) · 계층별 프로토콜 차이 · 5-State FSM · mechanism/policy 분리 · cmd_vel 파이프라인 |
| 2편 런타임편 | `moca-런타임-동작흐름-버튼한번이-바퀴까지.md` | 공통 런타임 골격 3가지 · 서빙 전체 체인(브라우저→모터) · FastAPI↔rclpy 통합(별도 스레드 executor) |

- 톤: 기술 해설(중립적), 기존 글 컨벤션(배경→접근→흐름요약→배운 점, `~하였습니다/~이다`) 준수.
- front matter: title 엠대시 부제 · tags(robotics 먼저) · `categories:["robotics"]` · summary · `cover.hidden` · `ShowToc/TocOpen`.
- 상호링크: 1편↔2편 양방향 + 기존 [오케스트레이터 글] 링크로 deep-dive 연결(중복 회피).
- 2편의 **FastAPI↔rclpy 통합(executor 별도 스레드 spin + `call_async` + spin↔asyncio 충돌 해결)** 은 기존 글에 없던 신규 내용.

---

## 2. 다이어그램 SVG 5종 (박스→SVG 변환)

**위치**: `static/images/diagrams/` — 터미널 카드 스타일(다크 #0c1117, JetBrains Mono, 신호등 점), 기존 `moca-opserver-orchestrator-flow.svg` 레퍼런스 준수.

| 파일 | 글 |
|---|---|
| `moca-3layer-architecture.svg` | 1편 |
| `moca-cmdvel-pipeline.svg` | 1편 |
| `moca-design-flow-summary.svg` | 1편 |
| `moca-serving-chain.svg` | 2편 |
| `moca-runtime-opserver-bridge.svg` | 2편 |

- 본문 박스 다이어그램은 전부 `{{< figure src="/images/diagrams/X.svg" alt="한글요약" >}}` 로 교체.
- 단순 화살표 흐름(하향/상향)은 SVG 대신 산문·번호목록으로 처리. 표·실코드는 유지.

---

## 3. 발행 폴더 혼선 복구 & myblog 중복 정리

- 최초 작성을 잘못된 **`~/myblog`** 에 함 → 이전 세션 결정대로 **발행은 `~/skong097.github.io`** 만 사용.
- 두 글 + 5 SVG 를 발행 repo 로 재작성(ASCII → SVG figure 변환 포함).
- `~/myblog` 에 잘못 만든 ASCII 중복 2편 삭제(이번 세션 생성분만). myblog robotics 18편으로 복귀.

---

## 4. 정적 검증 (hugo 미설치 + PaperMod 서브모듈 미초기화로 로컬 렌더 불가 → 정적 점검)

| 항목 | 결과 |
|---|---|
| SVG 5개 XML 유효성 | OK |
| figure src → 실제 파일 존재 | 5/5 |
| 내부 링크 → 타깃 .md 존재 | 깨진 링크 0 |
| slug/permalink override 없음 → URL=파일명 | 확인 |

→ 사용자가 직접 `git push origin main` → GitHub Actions 빌드 → **라이브 반영 완료 확인**.
공개 URL: `/posts/robotics/moca-시스템-아키텍처-웹운영화면부터-ros2-로봇까지/`, `/posts/robotics/moca-런타임-동작흐름-버튼한번이-바퀴까지/`.

---

## 후속 (보류)

- MOCA 나머지 영역(시퀀스 상세, 모객 BT, VLA 제조, Nav2)은 필요 시 같은 형식으로 이어서 포스트화 가능.
- 면접 학습노트(시스템설계/런타임)는 `moca_confluence/output/RAP` 에 보존.
