# AI 에이전트를 특정 분야의 전문가로 만드는 법 — 학습은 마지막 수단이다

> 상태: **소스 초안(재료)**. front matter 없음. 발행 시 한/영 쌍 + 다이어그램 SVG 변환 필요.
> 작성일: 2026-08-08.
> 재료 구분: **1장은 실측**(GitHub API 직접 조회, 재현 커맨드 첨부). **2~3장은 방법론 정리로, 조사가 아니라 판단이다** — 외부 출처를 붙이지 않았다. 발행하려면 근거 보강이 필요하다(8장 참조).
> 선행 초안: `blog-2026-08-07-personal-gpu-agent-collab-platform.md` (랜드스케이프 조사). 이 문서는 그 초안 5장의 갈래 **(b) 구상 제안형** 에 해당한다.

---

## 0. 한 줄 결론

**에이전트를 전문가로 만드는 병목은 대개 "아는 것"이 아니라 "틀렸음을 빨리 아는 것"이다.**
그래서 가중치를 바꾸는 학습은 사다리의 맨 아래에 있어야 하고, 대부분의 문제는 **검증기·도구·환경**을 붙이는 것으로 끝난다.

그리고 그 판단을 뒷받침하는 실측 사례가 하나 있다 — 이 계열에서 가장 앞서 있던 프로젝트가 **에이전트 자율성이 아니라 인간 유지보수 동력에서 멈췄다.**

---

## 1. 실측 — Hivemoot 현재 상태 진단 (2026-08-08)

`hivemoot/hivemoot` 은 선행 초안이 "빈칸에 가장 근접한 사례"로 지목한 프로젝트다. GitHub 위에서 역할을 가진 AI 에이전트 팀이 이슈를 열고, 토론하고, 코드를 쓰고, 투표로 결정하고, CI 통과 시 자동 머지한다. 핵심은 **"에이전트는 당신의 하드웨어에서, 당신의 API 키로 돈다"** 를 명시한다는 점이다.

### 1-1. 측정값

| 항목 | 값 | 비고 |
|---|---|---|
| 저장소 | `hivemoot/hivemoot` | HTTP 200 |
| 언어 / 라이선스 | TypeScript / Apache-2.0 | |
| 생성 | 2026-01-31 | |
| 스타 / 포크 / 워처 | 16 / 15 / **1** | 2026-08-07 조사분과 무변화 |
| 열린 항목 | 96 = **순수 이슈 75 + PR 21** | "이슈 96"만 쓰면 PR 이 섞인 수치 |
| 릴리스 | **0건** | 태그된 배포판 없음 |
| `pushed_at` | 2026-07-09 | **비기본 브랜치** 활동 |
| 기본 브랜치 마지막 실질 커밋 | **2026-05-31**, author `Queen` | 이후는 전부 dependabot (~06-12) |
| `hivemoot/colony` (데모) | 스타 4, 마지막 푸시 2026-03-26 | 쇼케이스가 먼저 정지 |

### 1-2. 진단: 살아 있지만 정지 상태(dormant)

죽지 않았다. 그러나 **약 10주간 실질 개발이 없다.**

- **`pushed_at` 이 실제보다 최신으로 보인다.** 저장소 목록에서 흔히 보는 "최근 푸시" 지표는 비기본 브랜치 활동까지 잡는다. 기본 브랜치 기준으로 보면 두 달 가까이 차이가 난다. → **활성도 판단에 `pushed_at` 을 쓰지 말 것. 기본 브랜치의 non-bot 커밋을 볼 것.**
- **워처 1명**이 결정적이다. 스타 16은 관심의 하한선이라 해석의 여지가 있지만, 워처 1은 **커뮤니티가 형성되지 않았다**는 직접 증거다.
- **포크 15 ≈ 스타 16** 은 이례적이다. 보통 포크는 스타의 10~20% 수준이다. 구경보다 **실제로 돌려보려 한 시도**의 비율이 높았다고 읽을 수 있다. (다만 이 해석은 단정할 근거가 아니다 — 8장 참조.)
- **릴리스 0 + 이슈 적체 96.** 프로덕트가 아니라 실험 단계에서 멈췄다.
- 마지막 실질 커밋의 author 가 **`Queen`** — 프로젝트가 설명하는 👑Queen 역할이 실제 커밋 author 로 찍혀 있다. 자기 저장소를 자기 에이전트로 개발했다는 주장의 직접 증거다.

**해석:** 실패한 프로젝트가 아니라 **1인(혹은 소수) 주도 실험의 소진**이다. 그리고 멈춘 지점이 중요하다 — 에이전트 자율 협업 기술이 무너져서가 아니라, **그 위에 얹힌 인간 유지보수 동력**이 끊겼다.

### 1-3. 이 진단이 선행 초안에 미치는 영향

판정표의 `P ✅ A ✅ C ❌` 는 그대로 유효하다. 다만 논지가 강화된다 — 선행 초안 4장의 *"빈칸은 아무도 생각 못 해서가 아니라 어려워서 비어 있다"* 에 **"가장 근접했던 사례조차 유지되지 못했다"** 는 실증이 붙는다.

발행 시 수치 표기는 `스타 16 / 이슈 96` 대신 다음을 쓴다:

```text
스타 16 / 워처 1 / 포크 15
열린 이슈 75 + 열린 PR 21 / 릴리스 0
기본 브랜치 마지막 실질 커밋 2026-05-31, 이후 dependabot 만
(2026-08-08 GitHub API 직접 조회)
```

---

## 2. 본론 — 에이전트의 "전문가화"를 어떻게 설계할 것인가

### 2-1. 먼저 진단: "전문가가 아니다"는 네 가지 다른 병이다

처방을 고르기 전에 **어느 결핍인지** 판별해야 한다. 이 구분을 건너뛰기 때문에 사람들이 곧장 파인튜닝으로 간다.

| 결핍 | 증상 | 처방 | 하면 안 되는 것 |
|---|---|---|---|
| **① 지식** — 모른다 | 사실·API·규격을 틀리게 말함 | 검색/RAG, 문서 주입 | 파인튜닝 (사실은 낡고, 가중치는 못 고침) |
| **② 절차** — 순서를 모른다 | 개별 단계는 맞는데 순서·누락으로 실패 | 워크플로·체크리스트·스캐폴딩 | 프롬프트만 길게 쓰기 |
| **③ 판단** — 고르지 못한다 | 둘 다 그럴듯하게 말하고 결정 회피. 트레이드오프 감각 없음 | 결정 사례 코퍼스, 소량 SFT, 리뷰 루프 | RAG (판단은 검색으로 안 나온다) |
| **④ 접지** — 확인하지 못한다 | **자신 있게 틀린다** | **도구·검증기·실행환경** | 더 큰 모델 |

**④가 압도적으로 흔하다.** 선행 초안 1장의 사건이 정확한 예시다 — 검색이 실존하지 않는 저장소(`hivemoot/hivemoot-agent`)를 상세한 설명과 함께 반환했고, GitHub API 로 확인하니 404였다. 이건 지식을 더 넣어서 고칠 수 없다. `curl` 한 줄이 고친다.

### 2-2. 비용 사다리 — 위에서부터 시도하고, 막히면 내려간다

| 단계 | 방법 | 비용 | 효과 | 언제 내려가나 |
|:--:|---|:--:|:--:|---|
| 1 | **평가셋 구축** (실사례 30~50건, 정답 있는 것) | 저 | — | **건너뛰지 말 것.** 없으면 개선 여부를 알 수 없다 |
| 2 | 컨텍스트 설계 (도메인 규범·용어·금지사항·few-shot) | 저 | 상 | 규범을 다 넣었는데도 틀릴 때 |
| 3 | **검증기·도구 부여** (컴파일러·시뮬·테스트·API·린터) | 중 | **최상** | 검증할 수 있는 게 없을 때 |
| 4 | 검색/RAG (도메인 코퍼스) | 중 | 상 | 사실은 맞는데 판단이 나쁠 때 |
| 5 | 메모리·경험 축적 (실패→원인→조치를 재주입) | 중 | 상 | 같은 실수를 반복할 때 |
| 6 | 워크플로 분해 + 적대적 검증 루프 | 중 | 상 | 한 번에 처리하기엔 태스크가 클 때 |
| 7 | **궤적 SFT** (성공 trajectory 증류) | 고 | 중 | 위 전부로 되긴 하는데 **너무 느리거나 비쌀 때** |
| 8 | RL / best-of-N (검증기 대상) | 고 | 상 | **싼 자동 검증기가 있을 때만** |
| 9 | 도메인 계속사전학습 | 최고 | 저~중 | 사실상 해당 없음 |

**하나만 남긴다면 3번이다.** 싼 검증기가 있으면 *평범한 모델 + 검증기 + 재시도* 가 *뛰어난 모델 단독* 을 이긴다. 전문성의 상당 부분은 아는 능력이 아니라 **틀렸음을 빨리 아는 능력**이고, 그건 가중치가 아니라 배관에 있다.

### 2-3. 어떤 자료를 먹일 것인가 — 가치 밀도 순

| 티어 | 자료 | 왜 | 쓰임 |
|:--:|---|---|---|
| **1** | **결정 기록** — 실패→근본원인→조치, 포스트모템, 코드리뷰 코멘트, 버그↔수정 쌍, 작업기록 | **판단**이 들어 있다. 공개된 게 거의 없다 | ③ 판단, SFT |
| **2** | **검증 가능한 산출물** — 코드+테스트, 시나리오, 로그/bag, 적합성 스위트 | 정답이 붙어 있어 평가셋·RL 신호가 된다 | ① 평가셋, ⑧ RL |
| **3** | **정전(正典) 레퍼런스** — 표준·규격, 교과서, API 문서 | 사실은 정확하나 판단이 없다 | RAG 전용 |
| **4** | 논문·기사·영상 | 넓이 확보. 노후화 빠름 | RAG, 배경 |
| **✗** | 일반 튜토리얼, 마케팅, **미검증 2차 보도** | 노이즈. 환각의 원료 | 넣지 말 것 |

**티어 1이 전부다.** 티어 3·4는 누구나 구하므로 차별화가 되지 않는다. 전문성은 *"Nav2 가 무엇인가"* 가 아니라 *"AMCL 이 흔들릴 때 `sigma_hit` 부터 의심한 이유"* 에 있고, 후자는 남의 문서에 없다.

### 2-4. 학습보다 효율적인 것들

1. **지식을 넣지 말고 검증기를 만들어라.** 답을 싸게 검사할 수 있으면 지능이 부족해도 결과는 정확해진다.
2. **교과서 대신 환경을 줘라.** bag 리플레이 + 시뮬레이터를 쥔 에이전트가, 문서를 다 읽은 에이전트를 이긴다.
3. **범위를 좁혀라.** 전문가화의 절반은 능력 향상이 아니라 **작업 범위 축소**다. "로보틱스 전문가"는 못 만들어도 "AMCL 파라미터 진단기"는 만들 수 있다.
4. **결정론으로 컴파일하라.** 린트 규칙이 될 수 있는 것은 모델 능력이 아니라 린트 규칙이어야 한다. 반복되는 실수는 학습시키지 말고 코드로 박는다.
5. **암기 대신 검색.** 변하는 사실(버전·수치·API)은 가중치에 넣지 않는다.
6. **실패를 자산으로 회수하라.** 실패마다 *증상/원인/조치* 를 한 줄 남기면 그게 다음 실행의 컨텍스트가 된다. 가장 싼 "학습"이다.
7. **5% 에만 사람을 붙여라.** 95% 를 자동화하고 위험한 결정에만 게이트를 거는 편이, 100% 를 잘하게 만드는 것보다 훨씬 싸다.

---

## 3. 이 블로그 / nextbrain 에의 적용

이 블로그의 파이프라인 — **KB Radar(외부 입력) + 작업기록·포스트(내부 입력) → nextbrain LLM wiki** — 는 이미 구조가 맞다. 부족한 것은 재료가 아니라 **되먹임 고리**다.

주목할 점: `content/posts/auto_drive/` 와 `작업기록_*.md` 는 대부분 *증상 → 가설 → 반증 → 조치* 구조다. **이것이 위 표의 티어 1이다.** 몇 년치가 이미 쌓여 있고, 이건 살 수 없는 자산이다.

순서:

1. **평가셋 30건** — 작업기록에서 *"당시 증상 → 실제로 옳았던 조치"* 를 문제/정답 쌍으로 추출한다. 후보: 오차보정, `sigma_hit`, colcon symlink 정적파일 404, AprilTag 정밀주차, 다중로봇 통행중재, GoAT 코드펜스 사고. **이게 없으면 이후 무엇을 해도 개선을 증명할 수 없다.**
2. **검증기 부여** — bag 리플레이, 파라미터 sanity 체커, 원본 엔드포인트 확인(`curl`). ④ 접지 결핍부터 막는다.
3. **티어 1 코퍼스 정규화** — 작업기록을 `증상 / 가설 / 반증 / 조치 / 근거` 스키마로 정리해 wiki 에 적재. 산문 그대로보다 검색·재사용성이 훨씬 높다.
4. **여기까지로 대부분 끝난다.** 그래도 느리거나 비싸면 그때 성공 궤적 SFT 를 검토한다. 그 전에는 하지 않는다.

---

## 4. 선행 초안과의 접속

선행 초안은 `P(개인 GPU) + A(에이전트 주체) + C(퍼블릭 공동 프로젝트)` 세 축을 동시에 만족하는 플랫폼이 없다고 판정했다. 이 문서는 그 빈칸을 **"왜 어려운가"** 쪽에서 한 겹 더 파고든다.

빠진 것은 프로토콜이 아니다. 여러 소유자의 에이전트가 하나의 공개 목표를 함께 만들려면, **각자의 기여를 신뢰 없이 검증할 방법**이 있어야 한다. 즉 2-2 사다리의 3번(검증기)이 **개인 단위 문제가 아니라 플랫폼의 1차 설계 요소**가 된다. BOINC 가 25년 전에 이걸 풀었던 방식(중복 계산 + 합의)이 참조점이고, 에이전트 기여는 계산 결과와 달리 결정론적으로 대조되지 않는다는 점이 난제다.

→ **한 문장:** 이 플랫폼의 핵심 부품은 스케줄러가 아니라 **채점기**다.

---

## 5. 포스트로 뽑을 때의 갈래

- **(a) 진단 단독** — 1장만. *"에이전트가 만든 저장소는 어떻게 멈추는가 — Hivemoot 10주 정지 기록"*. 짧고 실측 기반이라 검증 부담이 적다.
- **(b) 방법론 단독** — 2~3장. *"에이전트를 전문가로 만들 때 파인튜닝은 9번째 선택지다"*. 이 블로그의 파이프라인 서사와 바로 붙는다. **추천.**
- **(c) 통합** — 1→2→4장. 선행 초안의 후속편으로. 분량이 커지고 두 성격(실측/의견)이 섞이는 게 부담.

---

## 6. 발행 시 체크리스트

- [ ] 한/영 쌍 작성 (`.md` + `.en.md`, 동일 basename, 한글판에만 `slug:`)
- [ ] `date` / `categories` / `tags` / `draft` 두 파일 동일
- [ ] 카테고리 후보: `ai-agent`
- [ ] **코드펜스에 반드시 언어 명시** — 로그·수치 블록은 ` ```text `. 언어 없는 펜스는 Hugo 0.146 GoAT 훅에 잡혀 본문이 깨진다
- [ ] 다이어그램 SVG 후보 (터미널 카드 스타일, `static/images/diagrams/`)
      - `agent-expertise-ladder.svg` — 2-2 비용 사다리 (9단, 위=싸고 아래=비쌈)
      - `agent-deficit-quadrant.svg` — 2-1 네 결핍 → 처방 대응
      - 라벨이 한글이므로 **`-en.svg` 별도 제작 필요**
- [ ] 손수 쓴 SVG 는 cairosvg 로 PNG 래스터라이즈해 **라벨 겹침 육안 확인**
- [ ] 영문 포스트 추가 시 `/en/posts/` · `/en/` 항목 수 빌드 전후 대조
- [ ] 기존 한국어 URL 삭제·변경 0건 확인
- [ ] 선행 초안 발행본과 상대경로 교차링크
- [ ] 전 직장 업체명 노출 없음 확인

---

## 7. 측정 재현 (1장)

```bash
# 저장소 기본 정보 — pushed_at 이 아니라 아래 커밋 조회로 활성도를 판단할 것
curl -s https://api.github.com/repos/hivemoot/hivemoot | \
  python3 -c "import json,sys; d=json.load(sys.stdin); print(d['stargazers_count'], d['forks_count'], d['subscribers_count'], d['open_issues_count'], d['pushed_at'])"

# 이슈 96 의 분해 (순수 이슈 / PR)
curl -s "https://api.github.com/search/issues?q=repo:hivemoot/hivemoot+is:issue+is:open"
curl -s "https://api.github.com/search/issues?q=repo:hivemoot/hivemoot+is:pr+is:open"

# 기본 브랜치의 non-bot 마지막 커밋 — 활성도의 진짜 지표
curl -s "https://api.github.com/repos/hivemoot/hivemoot/commits?per_page=100" | \
  python3 -c "import json,sys; [print(x['commit']['author']['date'][:10], x['commit']['author']['name']) for x in json.load(sys.stdin) if 'dependabot' not in x['commit']['author']['name'].lower()][:5]"

# 릴리스
curl -s "https://api.github.com/repos/hivemoot/hivemoot/releases?per_page=3"

# 대조군 (엔드포인트 자체가 죽지 않았음을 확인)
curl -s -o /dev/null -w "%{http_code}\n" https://api.github.com/repos/bigscience-workshop/petals
```

---

## 8. 미검증 / 주의

- **2~3장 전체는 조사가 아니라 판단이다.** 외부 출처를 대지 않았고, 벤치마크로 뒷받침되지 않았다. 특히 다음은 발행 전 근거 보강이 필요하다:
  - 비용 사다리의 **순서**(특히 7·8단의 상대 위치)는 도메인과 검증기 가용성에 따라 달라진다. "항상 이 순서"로 단정하지 말 것.
  - *"평범한 모델 + 검증기가 뛰어난 모델 단독을 이긴다"* 는 조건부다. 검증기의 정밀도가 낮으면 성립하지 않는다. 인용 가능한 실증을 붙이거나, 조건을 명시할 것.
- **포크 15 ≈ 스타 16 의 해석**("돌려본 비율이 높다")은 **추정이다.** 봇 포크·템플릿 사용 등 다른 원인이 가능하다. 단정하지 말 것.
- Hivemoot 정지의 **원인은 확인하지 못했다.** 유지보수자에게 묻거나 이슈·논의 스레드를 읽지 않았다. "동력이 끊겼다"는 관측된 결과이지 확인된 원인이 아니다.
- 비기본 브랜치 활동(2026-07-09 푸시)의 **내용을 확인하지 않았다.** 재개 준비 중일 가능성을 배제할 수 없다.
- 선행 초안의 미검증 항목(Moltbook·Agentverse 에이전트 수, Petals 스웜 실가동률, Prime Intellect 의 소비자 GPU 허용 여부)은 **여전히 미해결**이다.
- `hivemoot/hivemoot-agent`, `Hivekeep`, `NemoClaw` 는 실존 미확인 — 인용 금지.
