# 작업기록 — 2026-06-14

> 블로그(`skong097.github.io` = `/home/gjkong/skong097.github.io`) KB Radar 키워드 확장 + 배포 repo 혼선 복구 세션.
> 핵심 교훈: **라이브는 `~/skong097.github.io` repo 에서만 배포된다.** `~/myblog`는 배포 안 되는 별도 stray repo(보존).

---

## 1. KB Radar — "LLM 학습 · 지식관리" 그룹 추가

**파일**: `data/kb_keywords.yaml`
**신규 그룹** `llm-knowledge` (categories 전체: paper·article·video·person, max_per_run 5)

키워드: `LLM wiki` · `Andrej Karpathy` · `안드레카파시` · `Obsidian` · `옵시디언`
(검색 적중률 위해 영문 + 한글 표기 병행)

**수집 실행** `scripts/run_kb_radar.sh --group llm-knowledge` → 신규 5건:
| 카테고리 | 제목 | 출처 |
|---|---|---|
| paper | Memory as Metabolism: A Design for Companion Knowledge Systems | arXiv |
| person | microgpt: 의존성 없는 200줄 파이썬 GPT 구현 (카파시) | Karpathy Blog |
| article | LLM Wiki 데스크톱 앱 (카파시 'LLM 위키' 패턴 구현) | GitHub |
| article | Obsidian + 로컬 LLM 플러그인 5선 (2026) | PromptQuorum |
| video | Build a 100% Private AI Second Brain: Local LLM + Obsidian | YouTube |

→ 라이브 반영 확인 (`/kb-radar/` 에 섹션 노출).

---

## 2. 배포 repo 혼선 발견 & 복구 (핵심 이슈)

**증상**: push 했는데 라이브 변동 없음.

**원인**: 키워드 추가·수집 작업을 잘못된 **`~/myblog`** repo(`github.com/skong097/myblog`)에서 함.
- 라이브는 **`~/skong097.github.io`** repo(루트 유저 사이트, GitHub Actions 자동 빌드)에서만 배포됨.
- `skong097.github.io/myblog/` 는 404 — myblog는 Pages 비활성. `run_server.sh`(`hugo server -D`)는 localhost 미리보기일 뿐 배포 기능 없음. "myblog 편집 → hugo server → github.io 자동 빌드" 연결 설정은 존재하지 않음.

**복구**: 진짜 repo를 클론(`~/skong097.github.io`)하고 변경분을 이관.
- `kb_keywords.yaml` 에 `llm-knowledge` 그룹 추가
- `kb_items.json` 에 신규 5건 머지 (id=URL SHA1, 중복 없음) → commit `968709f`, push, 라이브 확인.

---

## 3. myblog에 갇혀있던 작업 2건 이관

1. **LinkedIn 링크 제거** (myblog 06-13 커밋 `9525148` 이식) — `content/about/index.md` 연락처 버튼 + `hugo.yaml` socialIcons 에서 제거. commit `3b46f42`.
2. **06-13 KB 수집분** — myblog에만 있던 18건 중 중복 3건 제외 **신규 15건** `kb_items.json` 머지. commit `ee25173`.

→ 둘 다 라이브 반영 확인 (About LinkedIn 0건, KB Radar 항목 증가).

---

## 4. 블로그 지식 구조(비전) 명시

**파일**: `CLAUDE.md` 상단에 "이 블로그의 지식 구조 (비전)" 섹션 추가. commit `9c10108`.

> 바깥 지식(KB Radar 수집) + 내부 지식(포스트·프로젝트) → **nextbrain의 LLM wiki**(`~/nextbrain/wiki/`)로 수렴 → 개인 **세컨드 브레인** 파이프라인.

- **nextbrain** = `~/nextbrain` (`github.com/skong097/nextbrain`). Claude Code + Codex + Obsidian, Andrej Karpathy의 LLM Wiki 패턴. 구조: `raw/`(캡처)·`wiki/`(LLM 지식그래프)·`projects/`·`output/`.

---

## 5. 인증 트러블슈팅

`git push` 실패: HTTPS 비밀번호 인증 미지원. PAT를 넣었는데도 실패 → 에러의 `%EBskong097`(한글 UTF-8 바이트)로 **한글 IME 입력 오염**이 원인 판명. 영문 입력 상태에서 토큰을 URL에 직접 넣어 push 성공.

---

## 후속 (보류)

- 블로그 지식 → nextbrain `raw/` 자동 캡처 연결 (KB Radar 항목 → raw). 필요 시 진행.
- `~/myblog` 의 오늘 잘못 올린 커밋은 그대로 둠(사용자 요청).
