# 작업기록 — 2026-08-05

> 블로그를 **한국어(`/`) + 영어(`/en/`) 이중 언어**로 전환한 세션. Hugo 네이티브 다국어 + 헤더 KO↔EN 토글.
> 핵심 원칙: **기존 한국어 URL 311개 삭제·변경 0건** — 포트폴리오 QR·외부 링크 보존이 최우선 성공 기준이었고, 매 단계 회귀 게이트로 검증.
> 핵심 교훈: **JS 안 i18n 은 `{{ i18n "key" | jsonify | safeJS }}`** (`safeJS` 없으면 빌드는 통과하고 런타임에만 깨진다). **인라인 `<style>` 을 전역 CSS 로 옮길 때는 페이지 스코프에 의존하던 규칙을 분리**해야 한다.

---

## 0. 범위 결정

번역량이 비용의 대부분이라 먼저 범위를 잘랐다. 라이브 포스트 33편 ≈ 66,700단어.

| 대상 | 결정 |
|---|---|
| UI 문구 + 허브 페이지 5개 | **영문화** |
| 기존 포스트 33편 본문 | 번역 안 함. 한글 원문 유지 |
| `/en/posts/` | 한글 포스트를 그대로 노출 + 영문 안내 배너 |
| 드래프트 203편 / SVG 한글 라벨 | 손대지 않음 |
| 향후 새 포스트 | 한/영 쌍 작성 (`CLAUDE.md` 규칙화) |

산출물: `docs/superpowers/specs/2026-08-05-bilingual-blog-design.md`(설계), `docs/superpowers/plans/2026-08-05-bilingual-blog.md`(9태스크 계획).

---

## 1. Hugo 다국어 구조

`hugo.yaml` 을 `languages.ko` / `languages.en` 으로 분리.

```yaml
defaultContentLanguage: ko
defaultContentLanguageInSubdir: false   # ko 는 / 유지 → 기존 URL 무변경
```

- 언어 접미사 없는 기존 `content/**/*.md` 260개가 자동으로 `ko` 귀속 → **파일명 변경 0건**.
- 메뉴 `url:` 은 언어 중립(`/projects/`)으로 둔다. PaperMod 가 `absLangURL` 을 적용하므로 `/en/` 을 직접 쓰면 `/en/en/projects/` 가 된다.
- `params.mainSections: ["posts"]` **명시 필수** — EN 은 콘텐츠가 없어 Hugo 자동 추론이 빈 값을 내고 홈 그리드가 조용히 비어버린다.
- `params.displayFullLangName: true` → 토글이 "Ko/En" 대신 "한국어/English".

부수 효과: `/ko/` 별칭 페이지가 생긴다(noindex + canonical `/` + meta refresh). `defaultContentLanguageInSubdir: false` 의 Hugo 기본 동작이고 색인·기존 URL에 영향 없어 그대로 둠.

---

## 2. 언어 토글 — 테마 오버라이드가 필요했던 이유

PaperMod 에 언어 스위처가 **내장**되어 있으나 링크가 `site.Home.Translations` 다. 즉 **어느 페이지에서 눌러도 상대 언어의 홈으로 튕긴다** (`/en/about/` → `/about/` 이 아니라 `/`).

`layouts/partials/header.html` 을 테마에서 복사해 `lang-switch` 블록 한 곳만 교체:

```gotemplate
{{- $translations := .Translations }}
{{- if not $translations }}
    {{- $translations = site.Home.Translations }}
{{- end }}
{{- with $translations }}
```

- 재할당은 반드시 `=` (`:=` 는 내부 스코프에 새 변수를 만들어 폴백이 조용히 깨진다).
- 테마 원본 대비 diff 는 이 한 hunk뿐. 로고·다크모드 토글·메뉴 루프는 그대로 복사.

동작 확인:

| 위치 | 이동 |
|---|---|
| `/about/` | `/en/about/` |
| `/en/about/` | `/about/` |
| 한글 포스트(번역본 없음) | `/en/` (홈 폴백, 404 아님) |

---

## 3. i18n 문자열 분리 — `safeJS` 함정

`i18n/ko.yaml` · `i18n/en.yaml` 신설. PaperMod 가 자체 i18n 을 제공하고 **프로젝트 i18n 이 테마와 병합**되므로 커스텀 키만 넣는다(테마 키를 재정의하면 테마 번역이 깨진다).

교체 대상 — 화면에 실제로 보이는 24개만:

| 파일 | 개수 |
|---|---|
| `layouts/kb-radar/single.html` | 15 (칩 4·검색·페이저·빈상태 등) |
| `layouts/partials/hero-3d.html` · `hero-3d_dark.html` | 각 2 + CTA 링크 `relLangURL` |
| `layouts/shortcodes/about-3d.html` | 6 |
| `layouts/shortcodes/github-graph.html` | 3 |

`layouts/index.html` · `list.html` · `extend_head.html` 의 한글은 **전부 주석**이라 손대지 않음.

### 함정 (계획서가 틀렸던 부분)

계획서는 JS 안에 `{{ i18n "X" | jsonify }}` 를 쓰라고 했으나, 이 Hugo 빌드에서 **이중 이스케이프**가 일어난다.

```
{{ "hello" | jsonify }}           → "\"hello\""   ← 따옴표가 문자열에 박힌다
{{ "hello" | jsonify | safeJS }}  → "hello"       ← 정답
```

빌드는 통과하고 **런타임에만 깨져 화면이 빈다.** 11곳(`CAT_LABEL`×4, 빈상태 3, 페이저 2, untitled, ghProfile)에 `| safeJS` 추가. 백틱 템플릿 리터럴은 작은따옴표 문자열 연결로 변경(`${...}` 가 Go 템플릿과 충돌).

검증: `node --check` + 최소 DOM 스텁으로 **실제 실행**까지 확인. 렌더 결과 `CAT_LABEL={paper:"논문",...}`.

> `site.Data` 표기 유지 필수(`hugo.Data` 로 바꾸면 CI 빌드 실패). `layouts/kb-radar/single.html:127` 의 `site.Data.kb_items` 그대로.

---

## 4. `/en/posts/` 에 한글 포스트 차용

Hugo 는 언어별로 콘텐츠를 격리하므로 그냥 두면 `/en/posts/` 가 빈 페이지가 된다. `list.html` · `index.html` 에서 `site.Sites` 를 뒤져 ko 페이지를 빌려온다.

```gotemplate
{{- if and (eq site.Language.Lang "en") (or .IsHome (eq .Type "posts")) }}
  {{- if eq (len $pages) 0 }}
    {{- range site.Sites }}{{- if eq .Language.Lang "ko" }} ... {{- end }}{{- end }}
```

- `$isHome` 를 미리 뽑아둬야 한다 — `range site.Sites` 안에서는 `.` 이 Site 로 바뀌어 `.IsHome` 을 못 쓴다.
- 홈·posts 섹션으로만 한정(카테고리/태그 term 페이지까지 차용하면 언어가 뒤섞인다).
- **`content/posts/_index.en.md` 가 필요하다.** 없으면 EN 섹션 페이지 자체가 생성되지 않는다.
- 배너: `i18n` 키 `koPostsNotice` (ko 는 빈 문자열이라 한국어판엔 안 뜬다).

교차 사이트 컬렉션에 `.Paginate` 를 거는 게 리스크였으나 정상 동작 — ko/en 모두 총 33편·4페이지·페이지당 11행으로 일치.

---

## 5. projects/gallery 인라인 CSS 추출 — 계획 수정

영문판이 같은 마크업을 쓰므로 CSS 2벌 유지를 피하려 `assets/css/extended/custom.css` 로 옮겼다.

**그런데 `<style>` 을 통째로 옮기면 안 됐다.** `projects` 와 `gallery` 가 **둘 다** `header.post-header` 를 **서로 다른 값**으로 정의하고 있었다. 인라인이라 페이지 스코프가 유지되던 것이라, 전역으로 옮기면 (1) 전 사이트 모든 페이지에 적용되고 (2) 두 정의가 충돌한다.

| 대상 | 처리 |
|---|---|
| `.projects-*` `.project-card*` `.status-*` (35–177행) | → `custom.css` |
| `.gal-*` `#wasab/#moca/#araseo/#homecare` (15–48행) | → `custom.css` |
| `header.post-header` 계열 | **인라인 유지** |

### 검증 방법 (육안 대신 기계 판정)

순수 "이동" 작업이므로 규칙 집합 비교가 육안보다 강한 증거다. 페이지에 도달하는 `{셀렉터: 정규화된 선언}` 을 추출해 전후 대조.

| 페이지 | LOST | CHANGED | `header.post-header` |
|---|---|---|---|
| projects | 0 | 0 | 전후 모두 있음 |
| gallery | 0 | 0 | 전후 모두 있음 |
| posts | 0 | 0 | **전후 모두 없음** ← 누출 안 됨 |

카드 8장·갤러리 섹션 4개·앵커 4개 모두 동일.

> 검사기 작성 시 함정: PaperMod 의 스타일시트 링크는 `rel="preload stylesheet"` 라 `rel=stylesheet` 로만 매칭하면 **번들 CSS 를 통째로 놓친다**.

---

## 6. 영문 허브 페이지 5개

`content/{about/index,projects/_index,gallery/_index,kb-radar/index}.en.md` + `content/search.en.md`

공통 규칙:
- front matter 는 ko 에서 복사하되 **`url:` 필드는 삭제**. 남기면 `/en/` 접두사가 안 붙어 ko 와 URL 이 충돌한다.
- HTML 구조·클래스·`id`·`src`/`href` 는 한 글자도 바꾸지 않는다. 미디어 파일명에 한글이 있어도(`카페닌자.mp4`) 그대로.
- **갤러리 앵커 `#wasab #moca #araseo #homecare` 는 포트폴리오 QR 연결 대상** — 반드시 동일 유지.
- 포스트 링크(`/posts/...`)는 한국어 포스트를 가리킨 채로 둔다(영문 번역본이 없으므로 의도된 동작).
- About 의 `<style>` 은 추출 대상이 아니므로 통째로 복사. `{{< about-3d >}}` 쇼트코드는 이미 i18n 화되어 언어에 맞게 렌더된다.

결과: 홈 카드 17/17, 포스트 33/33, 프로젝트 카드 8/8, 갤러리 항목 44/44 — ko/en 일치.

EN 페이지에 남은 한글은 **UI 미번역이 아니라 데이터**다: 홈의 한글 포스트 제목(의도), `/en/projects/` 의 GitHub 레포 설명(`data/github_repos.json`), `/en/kb-radar/` 의 수집 기사(`data/kb_items.json`).

---

## 7. 검증 결과

| 항목 | 결과 |
|---|---|
| **기존 한국어 URL 삭제·변경** | **0건** (311개 전부 보존) |
| 빌드 WARN / ERROR | 0 / 0 |
| 총 URL | 311 → 329 (`/en/*` + `/ko/` 별칭) |
| 라이브 응답 | 한/영 11개 페이지 전부 200 |
| goat 코드펜스 | 7건 — **변경 전후 동일**(기존 문제, 이번 작업 무관) |

---

## 8. 커밋

브랜치 `feat/i18n-bilingual` 에서 작업 후 `main` 병합·푸시(배포 약 60초).

| 커밋 | 내용 |
|---|---|
| `93bca5a` | hugo.yaml 다국어 전환 |
| `fe19d4d` | 헤더 언어 토글 (페이지 대응 + 홈 폴백) |
| `abc72d6` | kb-radar 페이지네이션 (기존 미커밋분을 분리 커밋) |
| `a99d3c1` | 커스텀 템플릿 문자열 → i18n (`safeJS` 포함) |
| `ec3ffff` | EN 에 한국어 포스트 노출 + 배너 |
| `b08d35b` | projects/gallery CSS 추출 |
| `0173a4c` | CLAUDE.md 한/영 쌍 작성 규칙 |
| `48a008e` | 영문 허브 페이지 5개 |
| `e5958f8` | (머지 커밋) |
| `a05b135` | Zone.Identifier 잔재 31개 정리 + gitignore |
| `3b72671` | About 영문 이름에 로마자 병기 |

---

## 후속 / 남은 것

- **새 포스트는 한/영 쌍**: `<english-slug>.md` + `<english-slug>.en.md`. basename 이 같아야 Hugo 가 번역 연결. ko 파일에 `slug: <한글-슬러그>` 로 기존 URL 관례 유지. 상세는 `CLAUDE.md` 의 "포스트는 한/영 두 벌로 작성한다" 절.
- **기존 포스트 33편 소급 번역은 보류.** 필요해지면 편당 `.en.md` 를 추가하면 되고, 추가하는 순간 헤더 토글이 자동으로 같은 글의 반대 언어판을 가리킨다.
- **goat 렌더 문제 7편**(`blog-writer-agent-post`, `guard-brain-tool-use-agent`, `araseo-차선주행-스쿨존-hsv-색공간-구분`, `kevin-patrol-fleet-dashboard`, `moca-colcon-symlink-정적파일-404-해결`, `voice-iot-controller-overview`, `voice-iot-stt-llm-optimization`) — 언어 없는 코드펜스가 원인. 이번 범위 밖이라 미처리.
- SVG 다이어그램 영문판은 향후 새 포스트에만 적용(기존 것은 한글 라벨 유지).
