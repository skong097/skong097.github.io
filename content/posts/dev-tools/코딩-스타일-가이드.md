---
title: "📐 코딩 스타일 가이드"
date: 2026-03-21
draft: true
tags: ["dev-tools"]
categories: ["dev-tools"]
description: "| 항목 | 내용 | |------|------| | 버전 | 1.1 |"
---

# 📐 코딩 스타일 가이드

| 항목 | 내용 |
|------|------|
| 버전 | 1.1 |
| 작성일 | 2026-02-24 |
| 수정일 | 2026-03-18 |
| 적용 언어 | C++, Python |
| 참조 기준 | Google C++ Style Guide / PEP 8 |
| 대상 독자 | 전체 팀원 |

---

## 1. 개요 및 목적

본 문서는 팀 내 코드 일관성 유지 및 유지보수성 향상을 위한 C++ / Python 코딩 스타일 가이드를 정의합니다.

- **C++** 은 Google C++ Style Guide를 기준으로 합니다.
- **Python** 은 PEP 8을 기준으로 합니다.
- **ROS2 / 프로젝트 공통** 규칙은 별도 섹션(5장)에 정의합니다.

---

## 2. C++ 코딩 스타일 가이드

> 기준: [Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html)

### 2.1 네이밍 규칙

| 구분 | 규칙 | 예시 |
|------|------|------|
| 파일명 | snake_case, 소문자 | `my_class.cpp`, `utils.h` |
| 클래스 / 구조체 | PascalCase | `MyClass`, `DataPacket` |
| 함수 / 메서드 | PascalCase | `GetValue()`, `SendData()` |
| 변수 (지역 / 파라미터) | snake_case | `read_value`, `buffer_size` |
| 멤버 변수 | snake_case + 후위 언더스코어 | `device_id_`, `data_buffer_` |
| 전역 변수 | `g_` 접두사 + snake_case | `g_device_state` |
| 상수 (constexpr / const) | kPascalCase | `kMaxRetry`, `kBufferSize` |
| 매크로 | ALL_CAPS (가능하면 constexpr 사용) | `MAX_BUFFER_SIZE` |
| 네임스페이스 | 소문자 snake_case | `iot::network`, `iot::sensor` |
| 열거형 (enum class) | PascalCase (값은 kPascalCase) | `enum class DeviceState { kIdle, kActive };` |

> ⚠️ `using namespace std;` 는 헤더 파일에서 절대 사용하지 않습니다. 구현 파일에서도 최소화합니다.

---

### 2.2 포맷 & 들여쓰기

| 항목 | 규칙 |
|------|------|
| 들여쓰기 | 스페이스 2칸 (탭 금지) |
| 한 줄 최대 길이 | 80자 |
| 중괄호 | 같은 줄에 여는 중괄호 (K&R 스타일) |
| 포인터 / 참조 기호 | 타입에 붙임: `int* ptr`, `const std::string& name` |
| 빈 줄 | 함수 사이 1줄, 논리 블록 구분 시 사용 |

---

### 2.3 주석

| 종류 | 형식 | 설명 |
|------|------|------|
| 파일 헤더 | `// Copyright ...` | 저작권 및 파일 설명 |
| 클래스 / 함수 | 선언 위에 `//` 주석 | 목적, 파라미터, 반환값 설명 |
| 인라인 주석 | 코드와 같은 줄, 2칸 띄운 후 `//` | 복잡한 로직 설명 |
| TODO | `// TODO(담당자): 내용` | 미완성 작업 표시 |

---

### 2.4 헤더 파일

- `#pragma once` 를 헤더 가드로 사용합니다.
- Include 순서: **관련 헤더 → C 표준 → C++ 표준 → 서드파티 → 프로젝트 내부**. 각 그룹 사이 빈 줄을 둡니다.
- 헤더에는 최소한의 선언만 포함하고, 구현은 `.cpp` 에 작성합니다.

```cpp
#pragma once

#include <cstdint>
#include <string>
#include <memory>

#include "third_party/some_lib.h"

#include "project/internal_module.h"
```

---

### 2.5 메모리 & 포인터

| 규칙 | 설명 |
|------|------|
| 원시 포인터 최소화 | 소유권이 필요한 경우 스마트 포인터를 사용합니다. |
| `unique_ptr` | 단일 소유권 (기본 선택) |
| `shared_ptr` | 공유 소유권 (필요한 경우에만) |
| `new` / `delete` 직접 사용 금지 | `std::make_unique`, `std::make_shared` 를 사용합니다. |

---

### 2.6 클래스 설계

- 접근 지정자 순서: `public` → `protected` → `private`
- 각 접근 지정자 안에서: 타입 → 상수 → 생성자/소멸자 → 함수 → 멤버 변수 순으로 작성합니다.
- 복사 생성자 / 복사 대입 연산자가 필요 없는 경우 `= delete` 로 명시합니다.
- 상속 시 소멸자는 반드시 `virtual` 로 선언합니다.

---

## 3. Python 코딩 스타일 가이드

> 기준: [PEP 8 – Style Guide for Python Code](https://peps.python.org/pep-0008/)

### 3.1 네이밍 규칙

| 구분 | 규칙 | 예시 |
|------|------|------|
| 모듈 / 패키지 | 소문자 snake_case | `data_handler.py`, `utils/` |
| 클래스 | PascalCase | `DataHandler`, `DeviceManager` |
| 함수 / 메서드 | snake_case | `send_data()`, `read_value()` |
| 변수 | snake_case | `read_value`, `retry_count` |
| 상수 | ALL_CAPS | `MAX_RETRY = 3`, `TIMEOUT_SEC = 5` |
| 내부용 속성/메서드 | `_single_underscore` | `_internal_state`, `_connect()` |
| Name mangling (상속 충돌 방지용) | `__double_underscore` | `__private_buffer` |
| 매직 메서드 | `__dunder__` | `__init__`, `__repr__`, `__str__` |

> ⚠️ `__double_underscore`(더블 언더스코어)는 Python name mangling을 유발합니다.
> 단순 private 표현이 목적이라면 `_single_underscore`를 사용하세요.
> `__` 는 상속 계층에서 속성 이름 충돌을 방지해야 하는 경우로 한정합니다.

---

### 3.2 포맷 & 들여쓰기

| 항목 | 규칙 |
|------|------|
| 들여쓰기 | 스페이스 4칸 (탭 금지) |
| 한 줄 최대 길이 | 79자 |
| 최상위 함수 / 클래스 사이 | 빈 줄 2개 |
| 클래스 내 메서드 사이 | 빈 줄 1개 |
| 연산자 공백 | 이항 연산자 양쪽 공백. 단, 기본값 파라미터는 공백 없음 (`def f(x=0)`) |

---

### 3.3 타입 힌트 (Type Hint)

Python 3.10+ 기준으로 타입 힌트를 **필수** 적용합니다.

| 항목 | 규칙 |
|------|------|
| 함수 파라미터 | 모든 파라미터에 타입 힌트 명시 |
| 반환 타입 | `->` 반환 타입 필수 명시 |
| 컨테이너 타입 | `list[int]`, `dict[str, int]` (소문자, Python 3.10+) |
| 선택적 타입 | `Optional[str]` 또는 `str \| None` (Python 3.10+) |
| 클래스 속성 | `self._r: Optional[redis.Redis] = None` 형태로 선언 |

```python
# ✅ Good
def get_robot_state(self, robot_id: int) -> Optional[RobotState]:
    ...

def set(self, key: str, value: str, ttl_sec: int = -1) -> bool:
    ...

def get_alive_robots(self) -> list[int]:
    ...

# ❌ Bad
def get_robot_state(self, robot_id):
    ...
```

---

### 3.4 주석 & 독스트링

**인라인 주석**은 코드와 같은 줄에 최소 2칸 띄운 후 `#` 으로 작성합니다. 자명한 내용의 주석은 생략합니다.

**독스트링(Docstring)** 은 함수, 클래스, 모듈에 반드시 작성하며 **Google 스타일**을 사용합니다.

```python
def some_function(param_a: int, param_b: str = "default") -> bool:
    """함수의 역할을 한 줄로 요약합니다.

    Args:
        param_a: 첫 번째 파라미터 설명.
        param_b: 두 번째 파라미터 설명. 기본값은 "default".

    Returns:
        처리 성공 여부를 bool로 반환합니다.

    Raises:
        ValueError: param_a가 음수인 경우.
    """
    ...
```

---

### 3.5 Import 규칙

| 규칙 | 설명 |
|------|------|
| Import 순서 | ① 표준 라이브러리 → ② 서드파티 → ③ 로컬 패키지. 각 그룹 사이 빈 줄. |
| 개별 Import 권장 | `import os`, `import sys` (와일드카드 `import *` 금지) |
| 별칭 (alias) | 관행적 별칭 허용: `import numpy as np` |

```python
# ✅ Good
import os
import sys

import requests

from project.module import SomeClass

# ❌ Bad
from os import *
import sys, os
```

---

### 3.6 예외 처리

재난 환경 로봇 코드의 특성상 예외 처리 일관성이 특히 중요합니다.

| 규칙 | 설명 |
|------|------|
| `bare except` 금지 | `except:` 단독 사용 금지 |
| 구체적 예외 타입 명시 | `except RedisError as e:`, `except ValueError as e:` |
| `print` 금지 | 노드 코드에서 `print()` 대신 `logging` 또는 `rclpy` 로거 사용 |
| 예외 로깅 형식 | `logger.error(f"[모듈명] 상황 설명: {e}")` |
| 예외 무시 금지 | 불가피한 경우 `# noqa` + 주석으로 사유 명시 |

```python
# ✅ Good
try:
    result = self._r.ping()
except RedisConnectionError as e:
    logger.error(f"[Blackboard] Redis 연결 실패: {e}")
    return None

# ❌ Bad
try:
    result = self._r.ping()
except:
    pass
```

---

## 4. ROS2 / 프로젝트 공통 규칙

ROS2 노드, 토픽, 서비스, 패키지 네이밍은 아래 규칙을 따릅니다.

### 4.1 네이밍 규칙

| 구분 | 규칙 | 예시 |
|------|------|------|
| 패키지명 | snake_case | `ghost5_swarm`, `ghost5_interfaces` |
| 노드명 | snake_case | `blackboard_node`, `map_merger_node` |
| 토픽명 | `/snake/case` (슬래시 구분) | `/swarm/robot_0/pose`, `/swarm/merged_map` |
| 서비스명 | `/snake/case` | `/swarm/set_blackboard`, `/swarm/get_leader` |
| 액션명 | `/snake/case` | `/swarm/explore_frontier` |
| 메시지 타입 | PascalCase | `RobotStatus`, `VictimDetection` |
| 서비스 타입 | PascalCase | `ClaimFrontier`, `GetLeader` |
| 파라미터명 | snake_case | `redis_host`, `scan_resolution_m` |

### 4.2 파일 구조 규칙

| 구분 | 규칙 |
|------|------|
| Python 패키지 | `ament_python` 빌드 타입, `setup.py` + `setup.cfg` |
| C++ 패키지 | `ament_cmake` 빌드 타입, `CMakeLists.txt` |
| 설정 파일 | `config/` 디렉토리에 위치 (`.yaml`, `.json5`) |
| 런치 파일 | `launch/` 디렉토리에 위치 (`.launch.py`) |
| 테스트 파일 | `tests/unit/` 또는 `tests/integration/` |
| 작업 일지 | 하루 마지막 코드 작성 후 `.md` 파일로 기록 |

### 4.3 QoS 프로파일 규칙

토픽 QoS는 `ghost5_bringup/config/qos_profiles.py`의 프로파일을 사용하며, 임의 QoS 직접 생성을 금지합니다.

| 상황 | 사용 프로파일 |
|------|--------------|
| 로봇 위치 (고주파) | `POSE_QOS` |
| SLAM 맵 | `MAP_QOS` |
| 생존자 감지 | `VICTIM_QOS` |
| 군집 명령 / 선출 | `SWARM_CMD_QOS` |
| 센서 Raw | `SENSOR_QOS` |
| 고도맵 | `ELEVATION_QOS` |
| Inter-Robot 장애물 | `INTER_ROBOT_QOS` |

---

## 5. 언어별 비교 요약

| 항목 | C++ (Google Style) | Python (PEP 8) |
|------|-------------------|----------------|
| 들여쓰기 | 스페이스 2칸 | 스페이스 4칸 |
| 최대 줄 길이 | 80자 | 79자 |
| 클래스명 | PascalCase | PascalCase |
| 함수명 | PascalCase (`GetValue`) | snake_case (`get_value`) |
| 변수명 | snake_case | snake_case |
| 멤버 변수 | `snake_case_` (후위 언더스코어) | `self.snake_case` |
| 상수 | `kPascalCase` | `ALL_CAPS` |
| 내부 속성 | `snake_case_` (후위 언더스코어) | `_snake_case` (전위 언더스코어) |
| Include / Import 순서 | 관련 헤더 → C 표준 → C++ 표준 → 서드파티 → 내부 | 표준 → 서드파티 → 로컬 |
| 타입 힌트 | 정적 타입 (컴파일 타임) | 타입 힌트 필수 (`->`, `: type`) |
| 예외 처리 | `try-catch`, 구체적 타입 | `try-except`, 구체적 타입, bare except 금지 |
| 주석 / 문서화 | `//` + Doxygen 스타일 | `#` + Google Docstring |

---

*v1.0 → v1.1: Python private 변수 관례 보완, 타입 힌트 섹션 추가, 예외 처리 규칙 추가, ROS2 네이밍 섹션 추가, 언어별 비교표 확장*
